<%= form_for(@report_field) do |f| %>
  <% if @report_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_field.errors.count, "error") %> prohibited this report_field from being saved:</h2>

      <ul>
      <% @report_field.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
